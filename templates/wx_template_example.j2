{# WeChat Monitor Message Template - Grouped by Chat #}
{# Available variables: messages (list of dict) #}
{# Each message has: sender, chat, content, time, etc. #}

📱 **微信监控摘要**

共 {{ messages|length }} 条消息

{%- set grouped = messages|groupby('chat_name') %}
{%- for chat_name, chat_messages in grouped %}
**{{ chat_name }}**

{%- for msg in chat_messages %}
[{{ msg.time }}] {{ msg.sender|default('未知') }}: {{ msg.content }}

{%- endfor %}
{%- endfor %}

---
💡 触发条件: 内容超限 / 消息数超限 / 空闲超时
